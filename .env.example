# Server
PORT=3000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/armario-virtual

# OpenAI
OPENAI_API_KEY=sk-proj-...

# Replicate
REPLICATE_API_TOKEN=r8_...

# Auth (JWT secret for future auth middleware)
JWT_SECRET=genera-uno-con-node-e-crypto.randomBytes(64).toString('hex')


# File Upload
MAX_FILE_SIZE_MB=10
UPLOAD_DIR=uploads
